# Linux Resource Monitor Script
A simple Bash script to monitor system health including CPU, RAM, and Disk usage.

## Setup
1. Clone the repo
2. Run `chmod +x monitoring.sh`
3. Execute `./monitoring.sh`
